# ── Data pipeline ─────────────────────────────────────────
openpyxl>=3.1.0
boto3>=1.34.0
datasets>=4.0.0
huggingface_hub>=0.25.0

# ── Local training (QLoRA) ────────────────────────────────
torch>=2.3.0
transformers>=4.51.0
peft>=0.15.0
trl>=0.16.0
bitsandbytes>=0.45.0
accelerate>=1.0.0
scipy

# ── Optional ──────────────────────────────────────────────
# kaggle            # Kaggle CLI; needs KAGGLE_API_TOKEN in .env
# sagemaker>=2.200  # Only for SageMaker training jobs
